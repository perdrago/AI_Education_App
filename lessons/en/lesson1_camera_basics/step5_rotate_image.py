# ============================================================
# LESSON 1: Camera Basics - Step 5
# TITLE: Rotate Image
# TITLE_VI: Xoay Ảnh
# ============================================================

# Import camera, image processing, and display modules
import camera
import image
import display

# Step 1: Start the camera
# ✏️ INITIALIZE THE CAMERA
# __BLANK__ capture_camera = camera.Init_Camera()

print("[OK] Rotation ready!")

# Step 2: Rotate video in loop
while True:
    # ✏️ IN THE LOOP, GET A FRAME
    # __BLANK__ camera_frame = camera.Get_Camera_Frame(capture_camera = capture_camera)

    # ✏️ USE `ROTATE_IMAGE()` WITH `ANGLE=90` TO ROTATE THE IMAGE
    # __BLANK__ rotated_frame = image.rotate_image(input_image = camera_frame, angle = 90)

    # ✏️ DISPLAY THE ROTATED IMAGE
    # __BLANK__ display.Show_Image(camera_frame = rotated_frame)

# Step 3: Clean up
# ✏️ CLOSE THE CAMERA
# __BLANK__ camera.Close_Camera(capture_camera = capture_camera)

    pass  # Placeholder for blank lines