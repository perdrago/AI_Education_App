# ============================================================
# LESSON 3: Drawing & Shapes - Step 1
# TITLE: Draw Rectangle
# TITLE_VI: Vẽ Hình Chữ Nhật
# ============================================================

# Import camera and display modules
import camera
import display

# Step 1: Start the camera
# ✏️ INITIALIZE THE CAMERA
# __BLANK__ capture_camera = camera.Init_Camera()

print("[OK] Drawing ready!")

# Step 2: Draw rectangle in loop
while True:
    # ✏️ IN THE LOOP, GET A FRAME
    # __BLANK__ camera_frame = camera.Get_Camera_Frame(capture_camera = capture_camera)

    # ✏️ USE `DRAW_RECTANGLE()` WITH `X=100`, `Y=100`, `WIDTH=200`, `HEIGHT=150`, `COLOR="GREEN"`
    # __BLANK__ camera_frame = display.Draw_Rectangle(camera_frame = camera_frame, x = 100, y = 100, width = 200, height = 150, color = "green")

    # ✏️ DISPLAY THE IMAGE WITH RECTANGLE
    # __BLANK__ display.Show_Image(camera_frame = camera_frame)

# Step 3: Clean up
# ✏️ CLOSE THE CAMERA
# __BLANK__ camera.Close_Camera(capture_camera = capture_camera)

    pass  # Placeholder for blank lines