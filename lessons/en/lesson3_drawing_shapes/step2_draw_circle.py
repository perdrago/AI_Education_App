# ============================================================
# LESSON 3: Drawing & Shapes - Step 2
# TITLE: Draw Circle
# TITLE_VI: Vẽ Hình Tròn
# ============================================================

# Import camera and display modules
import camera
import display

# Step 1: Start the camera
# ✏️ INITIALIZE THE CAMERA
# __BLANK__ capture_camera = camera.Init_Camera()

print("[OK] Circle drawing ready!")

# Step 2: Draw circle in loop
while True:
    # ✏️ IN THE LOOP, GET A FRAME
    # __BLANK__ camera_frame = camera.Get_Camera_Frame(capture_camera = capture_camera)

    # ✏️ USE `DRAW_CIRCLE()` WITH `CENTER_X=320`, `CENTER_Y=240`, `RADIUS=100`, `COLOR="RED"`
    # __BLANK__ camera_frame = display.Draw_Circle(camera_frame = camera_frame, center_x = 320, center_y = 240, radius = 100, color = "red")

    # ✏️ DISPLAY THE IMAGE WITH CIRCLE
    # __BLANK__ display.Show_Image(camera_frame = camera_frame)

# Step 3: Clean up
# ✏️ CLOSE THE CAMERA
# __BLANK__ camera.Close_Camera(capture_camera = capture_camera)

    pass  # Placeholder for blank lines