# ============================================================
# LESSON 2: Image Processing - Step 2
# TITLE: Blur Effect
# TITLE_VI: Hiệu Ứng Làm Mờ
# ============================================================

# Import camera, image processing, and display modules
import camera
import image
import display

# Step 1: Start the camera
# ✏️ INITIALIZE THE CAMERA
# __BLANK__ capture_camera = camera.Init_Camera()

print("[OK] Blur effect ready!")

# Step 2: Apply blur in loop
while True:
    # ✏️ IN THE LOOP, GET A FRAME
    # __BLANK__ camera_frame = camera.Get_Camera_Frame(capture_camera = capture_camera)

    # ✏️ USE `APPLY_BLUR()` WITH `KERNEL_SIZE=15` TO BLUR THE IMAGE
    # __BLANK__ blurred_frame = image.apply_blur(input_image = camera_frame, kernel_size = 15)

    # ✏️ DISPLAY THE BLURRED IMAGE
    # __BLANK__ display.Show_Image(camera_frame = blurred_frame)

# Step 3: Clean up
# ✏️ CLOSE THE CAMERA
# __BLANK__ camera.Close_Camera(capture_camera = capture_camera)

    pass  # Placeholder for blank lines